// initial state
const state = {
  current: {}
}

// getters
const getters = {
}

// actions
const actions = {
}

// mutations
const mutations = {
  setEntity (state, newEntity) {
    state.entity = newEntity
  }
}

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations
}
